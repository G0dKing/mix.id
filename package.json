{
"scripts": {
    "start": "node backend/src/app.js",
    "build": "cd frontend && npm run build",
    "deploy": "npm run build && npm run start",
    "refresh": "npm install && cd frontend && npm install",
    "dev": "cd frontend && npm run dev",
    "client": "cd frontend && npm run build"
}
}
